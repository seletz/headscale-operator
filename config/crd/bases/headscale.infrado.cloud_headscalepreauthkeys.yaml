---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: headscalepreauthkeys.headscale.infrado.cloud
spec:
  group: headscale.infrado.cloud
  names:
    kind: HeadscalePreAuthKey
    listKind: HeadscalePreAuthKeyList
    plural: headscalepreauthkeys
    shortNames:
    - hspak
    - hspre
    singular: headscalepreauthkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.headscaleUserRef
      name: HeadscaleUser
      type: string
    - jsonPath: .status.userId
      name: UserID
      type: integer
    - jsonPath: .spec.reusable
      name: Reusable
      type: boolean
    - jsonPath: .spec.ephemeral
      name: Ephemeral
      type: boolean
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Status
      type: string
    - jsonPath: .status.expiresAt
      name: ExpiresAt
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: HeadscalePreAuthKey is the Schema for the headscalepreauthkeys
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HeadscalePreAuthKey
            properties:
              ephemeral:
                default: false
                description: Ephemeral indicates whether nodes using this key should
                  be ephemeral
                type: boolean
              expiration:
                default: 1h
                description: |-
                  Expiration is the duration after which the preauth key expires
                  Examples: 30m, 24h, 1h30m, 300ms, 1.5h (must be a valid Go duration string)
                  Valid time units are "s", "m", "h"
                pattern: ^([0-9]+(\.[0-9]+)?(s|m|h))+$
                type: string
              headscaleRef:
                description: HeadscaleRef is the name of the Headscale instance to
                  create the preauth key in
                type: string
              headscaleUserRef:
                description: |-
                  HeadscaleUserRef is the name of the HeadscaleUser resource to create the preauth key for
                  Either HeadscaleUserRef or UserID must be specified, but not both
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              reusable:
                default: false
                description: Reusable indicates whether the preauth key can be used
                  multiple times
                type: boolean
              secretName:
                description: |-
                  SecretName is the name of the secret to store the preauth key in
                  If not specified, defaults to the HeadscalePreAuthKey resource name
                type: string
              tags:
                description: Tags to automatically assign to nodes registered with
                  this preauth key
                items:
                  pattern: ^tag:[a-zA-Z0-9._-]+$
                  type: string
                type: array
              userId:
                description: |-
                  UserID is the ID of the user in Headscale to create the preauth key for
                  Either HeadscaleUserRef or UserID must be specified, but not both
                format: int64
                minimum: 1
                type: integer
            required:
            - headscaleRef
            type: object
          status:
            description: status defines the observed state of HeadscalePreAuthKey
            properties:
              conditions:
                description: conditions represent the current state of the HeadscalePreAuthKey
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              expiresAt:
                description: |-
                  ExpiresAt is the absolute time when the preauth key expires.
                  Computed by the controller from spec.Expiration at key creation time.
                format: date-time
                type: string
              keyId:
                description: KeyID is the ID of the preauth key in Headscale
                type: string
              userId:
                description: |-
                  UserID is the resolved user ID from Headscale.
                  Populated from the referenced HeadscaleUser's status or directly from spec.userId.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
        x-kubernetes-validations:
        - message: exactly one of spec.headscaleUserRef or spec.userId must be specified
          rule: (has(self.spec.headscaleUserRef) && self.spec.headscaleUserRef !=
            "") != (has(self.spec.userId) && self.spec.userId != 0)
    served: true
    storage: true
    subresources:
      status: {}
